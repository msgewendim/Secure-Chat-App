import { ToastOptions } from "react-toastify";

export const toastOptions : Partial<ToastOptions> = {
  position: "top-right",
  autoClose: 5000,
  pauseOnHover: true,
  draggable: true,
  theme: "dark",
}